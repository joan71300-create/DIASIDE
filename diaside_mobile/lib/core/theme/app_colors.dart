import 'package:flutter/material.dart';

class AppColors {
  // Primary Palette (Fresh & Healthy)
  static const Color primary = Color(0xFF5ED0A8); // Fresh Mint Green
  static const Color primaryDark = Color(0xFF45B08C);
  static const Color primaryLight = Color(0xFFE0F7EF);

  // Secondary/Accent (Warmth)
  static const Color secondary = Color(0xFFFFB74D); // Soft Peach/Orange
  static const Color secondaryLight = Color(0xFFFFF3E0);

  // Backgrounds
  static const Color background = Color(0xFFFFFFFF); // Pure White
  static const Color surface = Color(0xFFFAFAFA); // Very Light Grey for contrast
  static const Color surfaceVariant = Color(0xFFF3F4F6); // Inputs/Cards background

  // Text
  static const Color textPrimary = Color(0xFF1F2937); // Dark Grey (Almost Black)
  static const Color textSecondary = Color(0xFF6B7280); // Medium Grey
  static const Color textTertiary = Color(0xFF9CA3AF); // Light Grey

  // Status
  static const Color error = Color(0xFFEF5350);
  static const Color success = Color(0xFF66BB6A);
  static const Color warning = Color(0xFFFFA726);

  // UI Elements
  static const Color divider = Color(0xFFE5E7EB);
  static const Color border = Color(0xFFE5E7EB);
}